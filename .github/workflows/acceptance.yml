name: Acceptance
on: pull_request

jobs:
  integration-tests:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - windows-latest
          - ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - uses: ./setup-msbuild
        if: startsWith(matrix.os, 'windows')

      - name: Assert MSBuild exists on PATH
        if: startsWith(matrix.os, 'windows')
        run: where MSBuild.exe

      - name: Run MSBuild
        if: startsWith(matrix.os, 'windows')
        run: MSBuild -version

      - name: Install Java
        uses: actions/setup-java@v1
        with:
          java-version: 11

# TODO Add support to run without POM file. (Skip auto-version)
# TODO Add support for pom-file or no-version
#      - name: Run Maven
#        uses: ./maven
#        with:
#          args: --version
#        env:
#          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
#          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
