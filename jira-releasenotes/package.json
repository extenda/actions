{
  "name": "jira-releasenotes",
  "version": "0.0.1-local",
  "description": "Populate JIRA release notes from conventional commit conventions",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "test": "",
    "build": "ncc -m build src/index.js",
    "watch": "watch 'npm run build' ./src"
  },
  "dependencies": {
    "@actions/core": "^1.2.0",
    "jira-client": "^6.14.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5",
    "watch": "^1.0.2"
  }
}
