{
  "name": "conventional-release",
  "version": "0.0.1-local",
  "description": "Create a release based on conventional commit conventions and historic releases",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "test": "",
    "build": "ncc -m build src/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.2.0",
    "@actions/github": "^1.1.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5"
  }
}
