{
  "name": "wait-for-status",
  "version": "0.0.1-local",
  "description": "Wait for a GitHub status check to pass",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "test": "",
    "build": "ncc build src/index.js",
    "watch": "watch 'npm run build' ./src"
  },
  "dependencies": {
    "@actions/core": "^1.2.0",
    "@actions/github": "^1.1.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5",
    "jest": "^24.9.0",
    "watch": "^1.0.2"
  }
}
