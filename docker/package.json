{
  "name": "docker",
  "version": "0.0.1-local",
  "private": true,
  "description": "Docker build, tag, and push with default registry URL",
  "main": "dist/index.js",
  "dependencies": {
    "@actions/core": "^1.2.0"
  }
}
