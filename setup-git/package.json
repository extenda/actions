{
  "name": "setup-git",
  "version": "0.0.1-local",
  "private": true,
  "description": "Setup the Git CLI with credentials.",
  "main": "dist/index.js",
  "dependencies": {
    "@actions/core": "^1.9.1"
  }
}
