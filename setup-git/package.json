{
  "name": "setup-git",
  "version": "0.0.1-local",
  "private": true,
  "description": "Setup the Git CLI with credentials.",
  "main": "dist/index.js",
  "scripts": {
    "test": "",
    "build": "ncc build src/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.2.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5"
  }
}
