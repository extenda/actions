name: IAM permissions
description: Publish permissions to Hii Retail IAM
author: Extenda Retail
inputs:
  service-account-key:
    description: |
      The service account key which will be used for authentication
      to the secrets manager.
    required: true
  service-account-key-cluster:
    description: |
      The service account key used to authenticate to your cluster project
    required: true
  iam-definition:
    description: |
      The IAM YAML specification.
    required: false
    default: iam.yaml
  styra-tenant:
    description: |
      The styra workspace tenant
    required: false
    default: extendaretail
  iam-api-url:
    description: |
      The iam-api service url
    required: false
    default: https://iam-api.retailsvc.dev
runs:
  using: node12
  main: dist/index.js
